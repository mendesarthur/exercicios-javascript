var vetor = [2, 5, 10, 3]; // array são definidos entre colchetes

var posicao = 1;

console.log('posição 1: ' +vetor[posicao ]); //acessar posição de um array
console.log('comprimento:' + vetor.length); // .length é a propriedade que todo array tem que nos diz

for(var i = 0; i < vetor.length; i++){
    console.log(vetor[i]);
}